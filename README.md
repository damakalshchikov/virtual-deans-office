# Виртуальный деканат - Управление практикой студентов

## Описание

Веб-приложение «Виртуальный деканат» автоматизирует полный цикл управления
производственной практикой студентов: от планирования и распределения по организациям
до аттестации и формирования итоговых документов.

## Архитектура (6 модулей)

| Модуль | Описание |
|--------|----------|
| **Планирование** | Создание практик, этапов, назначение руководителей |
| **Распределение** | Вакансии, заявки, matching по компетенциям, распределение |
| **Выполнение** | Задания, сдача результатов, проверка куратором/преподавателем |
| **Аттестация** | Отчёты, дневники, характеристики, аттестационные листы, комиссии |
| **Администрирование** | Пользователи, роли, права, справочники (кафедры, организации, компетенции) |
| **Сервисный** | Профиль, компетенции студента, система уведомлений |

## Стек технологий

- **Backend:** Python 3, Flask 3.1, SQLAlchemy, Flask-Login
- **Frontend:** Bootstrap 5.3, Font Awesome 6.5, Jinja2
- **СУБД:** SQLite (по умолчанию) / PostgreSQL

## Установка и запуск

```bash
# 1. Установить зависимости
pip install -r requirements.txt

# 2. Создать БД и загрузить тестовые данные
python seed.py

# 3. Запустить сервер
python app.py
```

Приложение будет доступно по адресу: http://localhost:5000

## Тестовые учётные записи

| Логин | Пароль | Роль |
|-------|--------|------|
| admin | admin123 | Администратор |
| teacher | teacher123 | Преподаватель (Бычков С.Ю.) |
| teacher2 | teacher123 | Преподаватель (Иванов П.С.) |
| curator | curator123 | Куратор (Смирнова А.В.) |
| student | student123 | Студент (Кутенков С.Д.) |
| student2 | student123 | Студент (Эрендженова М.Н.) |
| student3 | student123 | Студент (Дамакальщиков М.А.) |

## Структура проекта

```
virtual-deans-office/
├── app.py                  # Точка входа, фабрика приложения
├── config.py               # Конфигурация (БД, SECRET_KEY)
├── models.py               # 20 моделей SQLAlchemy
├── helpers.py              # Утилиты (уведомления, matching)
├── seed.py                 # Генерация тестовых данных
├── requirements.txt
├── access/
│   └── decorators.py       # RBAC-декораторы (requires_role, has_permission)
├── auth/
│   └── routes.py           # Вход / выход
├── dashboard/
│   └── routes.py           # Главная, управление пользователями, справочники
├── planning/
│   └── routes.py           # CRUD практик и этапов
├── distribution/
│   └── routes.py           # Вакансии, заявки, matching, распределение
├── execution/
│   └── routes.py           # Задания, сдача, проверка
├── attestation/
│   └── routes.py           # Отчёты, характеристики, аттестация, комиссии
├── profile/
│   └── routes.py           # Профиль, компетенции, уведомления
└── templates/              # 39 Jinja2-шаблонов
    ├── base.html
    ├── login.html
    ├── dashboard/          # 8 шаблонов
    ├── planning/           # 4 шаблона
    ├── distribution/       # 5 шаблонов
    ├── execution/          # 5 шаблонов
    ├── attestation/        # 8 шаблонов
    └── profile/            # 4 шаблона
```

## Ролевая модель доступа (RBAC)

- **Администратор:** полный доступ ко всем модулям
- **Преподаватель:** планирование практик, создание заданий, аттестация, распределение
- **Куратор:** управление вакансиями, проверка заданий, характеристики
- **Студент:** заявки на вакансии, выполнение заданий, подача отчётов

## Алгоритм matching компетенций

При подаче заявки на вакансию система автоматически рассчитывает процент
совпадения компетенций студента с требованиями вакансии:

```
match% = (число компетенций, где уровень студента >= минимального) / (число требований) * 100
```
